# 四标的每日跟踪发布验收

交付范围为 PRD #34 与 R01–R07（#35–#41），包含必须先接入的持久同步恢复、ETF 协调器和 ETF 历史区间同步。用户已明确授权完成并验证后发布到 candlea.cn。原工作区未提交修改保留；发布在独立工作区进行。

## 需求与证据

| 任务 | 交付与已检查证据 |
| --- | --- |
| R01 / #35 / PR #42 | 真实截至日输入、完整收盘六指标、空值原因、四种宽度和独立异常状态；[每日验收](../design/rotation-daily/README.md) |
| R02 / #36 / PR #43 | 后端区间统计、默认一年／最长十年、连续持仓与初次成本不重置；[区间验收](../design/rotation-range/README.md) |
| R03 / #37 / PR #45 | 固定输入、源时间校验、执行权与管理记录；[采集说明](r03-reference-capture.md)及[浏览器证据](../design/rotation-capture/README.md) |
| R04 / #38 / PR #46 | 后端阶段切换、同日六指标、带方向 bps、固定参考和缺失降级；[双时点验收](../design/rotation-reference/README.md) |
| R05 / #39 / PR #47 | 真实留存日期索引、分页、历史选择与回测独立、请求竞争及焦点；[历史验收](../design/rotation-history/README.md) |
| R06 / #40 / PR #49 | 按原目标恢复、仅补未完成输入、去重／取消／重启和鉴权；[恢复验收](r06-rotation-recovery.md) |
| R07 / #41 / PR #51 | 价格／因子修正传播、参考不变、后续每日及完整回测发布、原范围恢复和迟到执行保护；[修正验收](r07-rotation-corrections.md) |

七票均有独立 PR、桌面／手机截图及明确标注来源的浏览器验收。R07 最后一次全量 race、web／机器人 race、前端构建和 37 项相关浏览器回归通过；最后发现的同步交接竞争已补充红绿验收并通过双轴复核。各票早期“后续待做”记录描述该票完成时的范围，以本表和后续票据为准。

最终镜像预检发现网站 Dockerfile 将前端平铺到 `/src`，无法解析构建必须使用的 DESIGN.md。修复为保持仓库的 `web/frontend` 目录层次，并复制单个规范文件；仍执行原有设计检查与构建，不跳过检查。修复后正式网站镜像构建通过。同步服务镜像构建通过。

## 隔离升级预演

2026-09-22 北京时间，使用生产一致性备份恢复到专用测试数据库，运行实际新同步服务镜像；生产库未修改。升级从 schema 3 到 10，后台生成 2026-09-21 完整四标的收盘六指标，生产默认 1200 有效值窗口不变。现有回测保留 2018-07-26 至 2026-09-21 的 1,980 条记录。默认区间为实际可用的 2025-09-22 至 2026-09-21。

14:45 原始参考无历史留存，参考、滑点均为空并说明原因，不从旧盘中表迁入。当前交易日收盘前回退到上一交易日，使用后台真实日历；查询不触发原始行情抓取。

## 发布与回退准备

数据库一致性备份、原容器 ID／镜像、原 Compose override、原 crontab 保存于服务器权限受限的 `/tmp/candela-release-20260921`。备份不提交。只更新 syncer、web；MySQL 和机器人容器保留。新增表及列为可重放的增量迁移；回退镜像时保留数据，不回删表。

外部采集脚本为 `deployments/capture-reference.sh`，要求调用方注入现有 SYNC_API_KEY。密钥通过 curl 标准输入传递，脚本按 Asia/Shanghai 计算原目标日期，并由后端判断交易日及采集窗口。宿主机 UTC 的 cron 时间为 `45 6 * * *`。保留原机器人 14:45 和 18:00 调度，不主动发送消息。启用情况另行记录，脚本存在不等于已启用。

## 当前发布状态与能力边界

待完成：依赖 PR 合并、线上服务替换、14:45 cron 安装及线上验证。公开 DNS 已确认 candlea.cn 根域名没有 A／AAAA 记录；当前 demo.candlea.cn 可进入原 Cloudflare Access 登录。根域名需要接入既有隧道并沿用登录保护，不能以关闭鉴权解决。根域名访问未验收，尚不声称目标完成。

历史 14:45 无法补取，真实参考档案从生产采集启用后开始；未在实际 14:45 窗口完成生产采集前，不声称真实双时点已齐全。#26 市场／申万历史同步不在本轮范围内，不随 ETF 前置切片关闭。

## 发布预检补充修复

- 普通浏览器配置误包含需要独立服务的修正套件；已按其他专用套件的方式排除。发现清单由 31 项恢复为原有 22 项，使用正式镜像静态产物运行，22 项全部通过（17.4s）。专用修正套件仍由其独立配置运行。
- 生产数据库会话使用北京时间，而每日状态读取以 UTC 标记时间；原先依赖 CURRENT_TIMESTAMP 的写入会偏移 8 小时。已将每日收盘／参考各阶段显式写为 UTC，发布时间 DATETIME 使用 UTC 文本避免驱动按连接时区转换。真实 HTTP 和 `+08:00` MySQL 会话回归先复现参考及收盘的 8 小时偏移，修复后与相关参考／收盘用例通过（7.164s）。原回测的既有时间处理保持不变。

## 最终预演与截图

时区修复后的完整 syncer race 通过（rotation 99.597s、syncrun 14.711s），两个镜像均按 c8181d6 构建成功。生产副本改用与线上相同的 `loc=Local` 和 `+08:00` 会话后，原回测时间保持一致；逐字段比较 1,980 条结果，最大数值差异 2.465e-32（MySQL JSON 导出／导入往返），远小于 1e-12 的验收阈值。目录逐字段一致。每日状态更新与实际发布时间相差不足 2 秒，四类新接口未认证均为 401。

最终只读预览使用该真实行情副本、实际新后端及正式镜像构建出的静态资源，临时签名登录由测试程序提供；不包含生产登录凭据或管理写入。顶部明确标注为生产副本，参考缺失。已检查 1440、820、390、320px 无整页横向溢出、无控制台错误，历史日期弹窗可用。预览在本机 `127.0.0.1:18083`，签名入口通过应用内浏览器打开，24 小时有效；不是公网发布证明。

[真实行情副本桌面截图](../design/rotation-release/desktop.png) · [手机截图](../design/rotation-release/mobile.png)

## Standards

只读复核通过，硬性规范违反 0 项、有实际影响的 Fowler smell 0 项。UTC 写入与读取标记一致；脚本标准输入传递密钥；Docker 依赖路径正确且环境文件不进入构建上下文。

## Spec

只读复核通过，未发现需求缺失、错误实现或范围扩张。保留设计构建检查；外部脚本沿用后端原目标和交易日校验；真实北京时间数据库会话回归验证时间修复；专用测试继续独立运行。

未解决发现：Standards 0 项，Spec 0 项。根域名及生产部署仍须按上文验收。

## 2026-09-22 发布门禁状态

页面基础 PR #33 已合并；R01–R07、必要前置及发布补充 PR #42–#52 仍未合并。发布补充为 [PR #52](https://github.com/fletcherlau/candela/pull/52)。后续批量改基线并合并操作被自动审批拒绝，理由为未明确授权这组共享主分支合并及较高回退成本；已向用户请求具体确认，未采用其他方式绕过。

`/tmp/candela-release-20260921/deploy.py` 已完成只读预检，默认不执行发布；仅 `--apply` 才替换服务，并先要求 PR #52 已合并。它检查原容器、源站环境和原调度未改变，重新备份、保存回退镜像，验证新版每日发布与原回测／目录，保留 MySQL／机器人容器，最后安装固定参考 cron。失败时恢复旧 syncer／web 镜像，保留所有新增数据表。当前 origin 仍保留 demo.candlea.cn；根域名需要单独完成路由与登录策略接入后才能验收。

当前生产服务、生产数据库和 crontab 均未修改；已运行的只有隔离副本、构建和只读预览。生产数据库备份、预演摘要及脚本在上述权限受限目录。原工作区不变。
