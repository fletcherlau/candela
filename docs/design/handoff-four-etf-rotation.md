# 四标的轮动页面 · 交接提示词

下面的提示词用于设计规范与回测基础合入 `master` 后的新会话。设计探索已结束，从正式业务页实施继续。

```text
请在 fletcherlau/candela 仓库，使用已经定稿的研究报告式设计规范，完成四标的轮动页面的视觉与交互迁移。请实际实现、运行并提供可查看的页面，不要只给方案。

工作起点：
- 从最新 origin/master 创建独立工作区和功能分支；旧工作区有其他任务的未提交修改，不要清理、覆盖或一并提交。
- 读取 AGENTS.md、web/frontend/AGENTS.md；通过 gh 阅读 issue #31 的当前正文与评论。
- 阅读 docs/design/DESIGN.md、docs/design/components.md、docs/design/preview/README.md、docs/design/workflow.md，以及 CONTEXT.md 和相关策略／接口实现。issue 折叠区是历史记录，不能覆盖当前规范。

已确认，不再重新选型：
- 中文标题 Noto Serif SC 300，中文阅读正文 400；英文及展示数字 Source Serif 4；操作控件使用 Noto Sans SC。
- 紧凑研究排版、净白纸面、12px 图版圆角、细边、无卡片阴影；文字与操作保持墨色，数据采用 A2 胭脂红／湖蓝等分类色，Lucide 线宽 1.5。
- 使用已有 React / TypeScript、Tailwind、shadcn/ui（Radix）和 ECharts。
- 页面用 ResearchTheme 包裹，复用已有组件和 researchTheme 参数。Dialog 自动继承主题；新增其他 portal 组件需单独接入并检查。
- 不恢复字体、配色、密度或纸面选择器；不复制样板的模拟数据、演示性故障开关或研究文案到正式页面。
- 若确需扩展参数，修改 DESIGN.md YAML，运行 design:generate 与 design:check；不要直接修改生成文件。

本轮页面与范围：
- 页面为 /strategies/four-etf-rotation，现有实现位于 web/frontend/src/pages/rotation.tsx。
- 在现有真实历史回测功能上迁移风格并整理内容层级，保留已有数据、交互和访问控制；不顺带修改算法、后端计算、交易、账户或机器人逻辑。
- 四标的：510880.SH 红利 ETF、518880.SH 黄金 ETF、159915.SZ 创业板 ETF、513100.SH 纳指 ETF。
- 页面顺序：策略身份与简要解释 → 紧凑数据状态摘要 → 收益与风险 → 净值／回撤／基准比较及历史持仓 → 规则、费用、来源与口径。
- 让首次访问者理解策略和风险，让复访者快速看清数据截至日与模型状态。保留时间范围、缩放、对照序列等已有功能；交互应联动相关指标。

数据要求：
- 使用现有 GET /api/rotation/backtest 的已发布结果，先核对实际响应；result.end 是数据截至日，updatedAt 是发布更新时间，二者分开。
- 持仓与权重是历史回测模型的结果，不是实际账户仓位、今日实时信号或成交记录。不要从持仓变化编造排名和变化原因。
- ready/pending/computing/syncing/failed/stale 等状态按实际契约展示；旧数据可以保留并注明滞后／失败；未知不填零，也不解释为“维持不变”。
- 收益、回撤、时间区间与成本采用同一结果口径；不要混用旧定投实验、历史简报的固定收益数字或样板数据。
- 当前页面默认近一年，旧简报要求默认完整历史。首次迁移保留近一年默认与完整历史入口，并在交付时明确这项待确认的业务差异，不把视觉定稿当作业务选择。
- #31 的每日排名／变化原因、净值与定投资金切换仍属于整体需求，但现有回测接口不提供相应数据。先交付真实回测页迁移，明确列出这些后续数据契约与接入缺口；不要用假数据补齐或宣称整个 issue 已完成。

验收与交付：
- 使用适用的仓库 skills。不要重新开启审美探索；先检查代码和接口，再实施。
- 验证桌面、820px、390px、320px 布局，实际字体、键盘操作、焦点、弹窗、范围切换和图表联动。
- 用隔离测试数据覆盖正常、加载、空数据、失败、滞后和缺失值；测试数据不能替换生产接口，也不能通过移除鉴权预览。
- 运行 design:check、构建、test:design 及受影响的轮动页测试。官方 lint 已记录的 13 条 orphaned-tokens 警告是代码侧颜色引用，不能当成新错误，也不能忽略新增警告。
- 提供可用预览与桌面／手机截图。分享稳定的服务端地址，不沿用旧会话临时转发端口。过程截图不要大量提交到 Git。
- 更新 issue #31 的实施进度与未完成项，提交独立 PR；本轮不自动部署生产、不关闭仍有未完成业务需求的 issue。
- 最终说明改了什么、实际验证结果和剩余数据缺口，并给出可直接打开的页面入口。
```
